{
  "name": "corrad-php-manual",
  "private": true,
  "scripts": {
    "docs:sidebar": "node scripts/generate-sidebar.js",
    "docs:dev": "npx vitepress dev docs",
    "docs:build": "npx vitepress build docs"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
